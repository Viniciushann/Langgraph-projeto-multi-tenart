# ========================================
# TEMPLATE DE VARIÁVEIS DE AMBIENTE
# ========================================
# IMPORTANTE:
# 1. Copie este arquivo para .env
# 2. Preencha com suas credenciais reais
# 3. NUNCA commite o arquivo .env!
# ========================================

# ========== EVOLUTION API (WhatsApp Business) ==========
# URL da sua Evolution API instalada
WHATSAPP_API_URL=https://evolution.centrooestedrywalldry.com.br

# API Key da Evolution (obtida no painel)
WHATSAPP_API_KEY=sua-chave-evolution-api-aqui

# Nome da instância do WhatsApp
WHATSAPP_INSTANCE=Centro_oeste_draywal

# ========== OPENAI API ==========
# Chave da API OpenAI (necessária para GPT-4, Whisper, Embeddings)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...

# ========== SUPABASE ==========
# URL do projeto Supabase
# Encontre em: Settings > API > Project URL
SUPABASE_URL=https://seu-projeto.supabase.co

# Chave anon/public do Supabase
# Encontre em: Settings > API > Project API keys > anon public
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ========== REDIS ==========
# Para desenvolvimento local use: localhost
# Para produção (Hetzner) use: redis (nome do container/service)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ========== POSTGRESQL (Memória de Conversas) ==========
# String de conexão do PostgreSQL (Supabase)
# Formato: postgresql://user:password@host:port/database?sslmode=require
# Encontre em: Settings > Database > Connection string > URI
POSTGRES_CONNECTION_STRING=postgresql://postgres:sua-senha@db.seu-projeto.supabase.co:5432/postgres?sslmode=require

# ========== GOOGLE CALENDAR ==========
# Nome do arquivo de credenciais do Google
# Deve estar na raiz do projeto
GOOGLE_CALENDAR_CREDENTIALS_FILE=credentials.json

# ========== CONFIGURAÇÕES DO BOT ==========
# Número do WhatsApp do bot (com DDI e DDD, sem espaços ou caracteres especiais)
BOT_PHONE_NUMBER=556292745972

# Delay em segundos para agrupar mensagens múltiplas
MESSAGE_GROUP_DELAY=25

# Tamanho máximo de cada fragmento de resposta
MAX_FRAGMENT_SIZE=300

# ========== CONFIGURAÇÕES DO AGENTE ==========
# Timeout em segundos para respostas do agente
AGENT_TIMEOUT=60

# Número máximo de tentativas em caso de erro
MAX_RETRIES=3

# Habilitar persistência de memória de conversas
ENABLE_MEMORY_PERSISTENCE=True

# ========== APLICAÇÃO ==========
# Ambiente: development | production | staging
ENVIRONMENT=production

# Porta em que a aplicação vai rodar
PORT=8000

# Host (use 0.0.0.0 para aceitar conexões externas)
HOST=0.0.0.0

# Nível de log: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO

# ========== SEGURANÇA ==========
# Chave secreta para JWT e criptografia (mínimo 32 caracteres)
# Gere com: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=gere-uma-chave-secreta-muito-longa-e-aleatoria-aqui

# Origens permitidas para CORS (separadas por vírgula)
CORS_ORIGINS=https://evolution.centrooestedrywalldry.com.br,https://seu-dominio.com

# ========== TRAEFIK (para deploy com Traefik) ==========
# Domínio onde o bot ficará acessível
DOMAIN=seu-dominio.com
# O bot ficará em: https://bot.seu-dominio.com

# ========== TÉCNICO ==========
# Número do técnico para notificações (com DDI e DDD)
TELEFONE_TECNICO=556298540075
